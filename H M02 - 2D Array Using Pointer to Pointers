#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main()
{
	char** str_arr;
	char* result=(char*)malloc(sizeof(char)*100) ; //don't forget to allocate sufficient memory to result string
	int i;
	//your code here
 str_arr=(char**)malloc(sizeof(char*)*5) ; 
    for(int i=0;i<5;i++) str_arr[i]=(char*)malloc(sizeof(char)*10);
      for(i=0;i<5;i++) {
        scanf("%s",str_arr[i]);
    }
	for (i = 0; i < 5; i++)
		strcat(result, str_arr[i]);
	puts(result);
	return 0;
}
